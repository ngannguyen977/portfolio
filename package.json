{
  "name": "ngan-resume",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "clean":"rm -rf dist/* docs/*",
    "build": "npm run clean && vite build",
    "prepublish": "cp -r dist/* docs",
    "publish": "npm run build && npm run prepublish && git add -A && git commit -m 'chore: update materials' && git push origin master",
    "preview": "vite preview"
  },
  "dependencies": {
    "boostrap": "^2.0.0",
    "bootstrap": "^5.3.3",
    "gsap": "^3.12.5",
    "vue": "^3.4.29",
    "vue-router": "^4.3.3"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.5",
    "@vitejs/plugin-vue-jsx": "^4.0.0",
    "sass-embedded": "^1.77.8",
    "vite": "^5.3.1",
    "vite-plugin-vue-devtools": "^7.3.1"
  }
}
